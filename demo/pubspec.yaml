name: demo
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  install_plugin: ^2.0.1
  toast:
#    git:
#      url: https://github.com/crazyball666/flutter_packages.git
#      path: toast
    path: "../toast/"
  native_util:
    path: "../native_util/"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

